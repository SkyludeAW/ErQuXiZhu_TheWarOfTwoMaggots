using UnityEngine;

public class 效果 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        效果出发.action += OnEffectTriggered;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
